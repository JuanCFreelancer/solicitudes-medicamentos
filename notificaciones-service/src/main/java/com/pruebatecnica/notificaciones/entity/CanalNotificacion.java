package com.pruebatecnica.notificaciones.entity;

/** Medio por el que se entrega una notificación. Hoy solo correo; agregar otro canal no cambia el contrato. */
public enum CanalNotificacion {
    EMAIL
}
